# CAUC 校园网自动登录 — 产品需求（PRD）

> 由 `粗糙需求.md` 整理，并经 `对话 html/index.html` 产品选题更新（2026-05-29）。  
> **技术栈与实现路线不在本文** → 见 `plan/技术方案.md`（阶段 2 撰写）。

---

## 1. 背景与目标

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 要解决的问题 | [已定] | 连接 **CAUC-WIFI** 后需打开校园网门户 `http://192.168.4.252/` 并手动输入账号密码才能上网 |
| 成功时用户得到什么 | [已定] | 连上指定 WiFi 后**无需手动操作**即可恢复外网；页面出现「您已经成功登录」即视为成功 |

---

## 2. 用户与典型场景

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 目标用户 | [已定] | 民航大学师生（macOS）；首装者本人，并希望做成**可分享模板**供同学各自配置账号 |
| 典型场景（1–3 个） | [已定] | ① 开机后已连 CAUC-WIFI，希望自动过门户；② 从校外回校连上 CAUC-WIFI，希望立刻能上网；③ 睡眠唤醒后 WiFi 重连，希望自动重登而不反复找页面 |

---

## 3. 功能范围（用户能做什么）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 核心流程 | [已定] | 检测到 **CAUC-WIFI**（一连上即触发检查）→ 若需登录则打开门户 → 自动填账号密码 → 点击登录 → 确认成功 |
| 关键页面/状态 | [已定] | 门户登录页（账号 `DDDDD`、密码 `upass`、登录 `0MKKey`）；成功态含「您已经成功登录」 |
| 边界与例外 | [已定] | **已能上网**：后台静默检查，仅必要时才打开门户；**失败**：macOS 通知并暂停自动重试直至用户处理；**注销**：自动化绝不点击注销按钮 |

---

## 4. 体验与交互原则

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 交互方式 | [已定] | **后台无感**为主（headless，不抢焦点）；已登录时不打扰 |
| 视觉/氛围期望 | [已定] | 以「无感上网」为主，尽量不打扰日常用电脑 |
| 进度与反馈 | [已定] | **成功**：简短系统通知「已成功登录」；**失败**：系统通知并停止重试，等待用户介入 |

---

## 5. 明确不做（范围外）

| 项 | 说明 |
| --- | --- |
| 非 CAUC-WIFI 网络 | 不在其它 WiFi / 有线环境下自动登录 |
| 主动注销 | **禁止**自动化流程点击门户「注销」；页面上可保留按钮，程序不点即可 |
| Windows / Linux / 手机 | 首版仅面向 **macOS** |
| 集中代管他人密码 | 不做云端账号池；「同学也能用」= 各自克隆项目、各自本机配置文件 |

---

## 6. 产品决策记录（HTML 提交后填写）

| 题号 | 问题（产品向） | 选择 | 用户可理解含义 |
| --- | --- | --- | --- |
| q_trigger_when | 什么时候希望程序自动帮你登录？ | `on_wifi_connect` | 一连上 CAUC-WIFI 就立刻尝试 |
| q_already_logged_in | 如果其实已经能正常上网了，你希望怎样？ | `check_silent` | 后台静默检查一次，只有真需要登录时才打开门户 |
| q_failure_feedback | 自动登录失败时，你希望怎样？ | `notify` | 发 macOS 通知，并停止自动重试直到我处理 |
| q_success_feedback | 自动登录成功时，你希望怎样？ | `notify_brief` | 简短系统通知「已成功登录」，窗口可自动关掉 |
| q_credential_handling | 账号密码希望怎样保管？ | `local_config` | 放在本机配置文件里，我自己管文件权限，方便改密码 |
| q_browser_visibility | 自动登录时，浏览器窗口应该怎样？ | `headless` | 尽量后台完成，不要抢焦点 |
| q_scope_devices | 这套工具打算怎么用？ | `share_template` | 希望做成模板：同学各自填自己的账号也能用 |
| q_logout_safety | 门户页上的「注销」按钮，你希望怎么处理？ | `never_click_only` | 程序绝不点击注销即可，页面上保留按钮 |

---

## 7. 成功标准（需求级验收）

完成本 PRD 即表示：

1. 在 **CAUC-WIFI** 下，用户无需手动打开 `192.168.4.252` 即可完成门户登录并访问外网。
2. 一连上 CAUC-WIFI 即触发流程；若已能上网则静默跳过，不反复弹门户。
3. 自动化流程**不会**点击注销；失败时 macOS 通知并暂停重试；成功时简短通知。
4. 凭据存于**本机配置文件**（不入 Git）；仓库可作为模板供他人各自配置。

（阶段 2 的 `plan/Mxx-<模块名>.md` 拆实现步骤；`plan/技术方案.md` 定技术栈。）

---

## 8. 技术实现（待 plan 阶段）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 技术栈 | 见 plan | 由 Agent 在 `plan/技术方案.md` 根据 PRD 撰写 |
| 部署与运行 | 见 plan | 含 macOS 开机自启、WiFi 监听、无头浏览器自动化等 |
| 数据与存储 | 见 plan | 本机配置文件存账号密码；`.gitignore` 排除敏感文件 |
